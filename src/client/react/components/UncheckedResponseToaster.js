import React from 'react';
import { Toaster, Position } from '@blueprintjs/core';

const UncheckedResponseToaster = Toaster.create({
	position: Position.TOP
});

export default UncheckedResponseToaster;
